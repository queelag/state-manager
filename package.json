{
  "author": "Dario Sechi",
  "devDependencies": {
    "@microsoft/api-extractor": "^7.24.0",
    "@queelag/core": "^1.1.12",
    "@rollup/plugin-typescript": "^8.3.2",
    "@vitest/coverage-c8": "^0.24.5",
    "@vitest/ui": "^0.24.5",
    "dayjs": "^1.11.3",
    "prettier": "^2.6.2",
    "rollup": "^2.73.0",
    "rollup-plugin-terser": "^7.0.2",
    "superstruct": "^0.16.0",
    "tslib": "^2.4.0",
    "typedoc": "^0.22.15",
    "typescript": "4.6.4",
    "vitest": "^0.24.5"
  },
  "license": "MIT",
  "main": "index.cjs",
  "module": "index.js",
  "name": "@queelag/state-manager",
  "peerDependencies": {
    "@queelag/core": ">= 1.0.12"
  },
  "repository": "https://github.com/queelag/state-manager",
  "scripts": {
    "test": "yarn vitest run",
    "build": "rm -rf dist && yarn rollup -c && yarn api-extractor run --local",
    "tarball": "yarn version && cp package.json README.md dist/. && cd dist && yarn pack",
    "deploy": "yarn test && yarn build && yarn tarball && yarn publish dist/*.tgz"
  },
  "sideEffects": false,
  "version": "1.0.10"
}
